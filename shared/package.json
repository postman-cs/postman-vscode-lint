{
  "name": "postman-lint-server-shared",
  "version": "0.1.0",
  "description": "Shared types and constants for Postman Lint Server",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "rimraf lib"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  },
  "dependencies": {
    "vscode-languageserver-types": "^3.17.0"
  }
}
