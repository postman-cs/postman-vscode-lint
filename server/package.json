{
  "name": "postman-lint-server-server",
  "version": "0.1.0",
  "description": "Language Server for Postman API Governance validation",
  "main": "lib/server.js",
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "clean": "rimraf lib",
    "test": "jest"
  },
  "dependencies": {
    "postman-lint-server-shared": "file:../shared",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^18.0.0",
    "jest": "^29.0.0",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
